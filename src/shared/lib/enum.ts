export enum RootPaths {
    AUTH = '/auth',
    MAIN = '/main',
    PROFILE = '/profile',
    CREATE = '/create',
    ANOTHER = '*',
}

export enum CreatePaths {
    REGION = 'region',
    TARIFF = 'tariff',
    PAYMENT = 'payment',
    DONE = 'done',
}