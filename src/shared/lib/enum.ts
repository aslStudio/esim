export enum RootPaths {
    AUTH = '/auth',
    MAIN = '/',
    PROFILE = '/profile',
    ESIM = '/esim',
    CREATE = '/create',
    ANOTHER = '*',
}

export enum CreatePaths {
    REGION = 'region',
    TARIFF = 'tariff',
    PAYMENT = 'payment',
    DONE = 'done',
}